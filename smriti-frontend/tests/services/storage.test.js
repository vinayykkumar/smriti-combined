/**
 * Tests for storage service.
 */
import * as storage from '../../src/services/storage';

describe('Storage Service', () => {
  it('has storage functions', () => {
    expect(storage).toBeDefined();
  });
});
